# Dock layout for personal machines — one app name per line, top to bottom
# becomes left to right. Reorder lines to reorder the Dock; # comments and
# blank lines are ignored. Apps not installed yet are skipped with a warning.
# A full /path/to.app also works for apps outside the standard folders.
# Applied by setup.sh --dock (declarative: the Dock becomes exactly this list).

iTerm
Visual Studio Code
Claude
Google Chrome
Firefox
Obsidian
Tower
Messages
System Settings
